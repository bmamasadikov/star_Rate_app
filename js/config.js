// Cloud configuration. Leave both values empty to run in local-only mode (no accounts, data stays in the browser).
// To enable accounts and the shared registry: create a free Supabase project, run supabase/schema.sql in its SQL editor,
// then paste the project URL and the anon (public) key here.
window.CLOUD_CONFIG = {
  url: "",
  anonKey: ""
};
